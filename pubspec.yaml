name: movies_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.9.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  animate_do: ^4.2.0
  card_swiper: ^3.0.1
  cupertino_icons: ^1.0.8
  dio: ^5.9.0
  drift: ^2.29.0
  drift_flutter: ^0.3.0
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  flutter_dotenv: ^6.0.0
  flutter_launcher_icons: ^0.14.4
  flutter_riverpod: ^3.0.3
  go_router: ^17.1.0
  intl: ^0.20.2
  path_provider: ^2.1.5
  equatable: ^2.0.7
  url_launcher: ^6.2.5
  font_awesome_flutter: ^11.0.0
  shimmer_animation: ^2.2.2+1
  # flutter_local_notifications: ^19.5.0
  loading_animation_widget: ^1.3.0
  video_player: ^2.10.1
  youtube_player_flutter: ^9.1.3
  flutter_native_splash: ^2.4.7
  flutter_staggered_grid_view: ^0.7.0
  audioplayers: ^6.5.1
  google_fonts: ^8.0.0
  shared_preferences: ^2.2.2
  animated_theme_switcher: ^2.0.10
  share_plus: ^12.0.1
  change_app_package_name: ^1.5.0
  local_auth: ^3.0.1
  smooth_page_indicator: ^2.0.1
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/logo_app/logo1.png"
  min_sdk_android: 21
flutter_native_splash:
  color: "#000000" # Color de fondo
  image: "assets/logo_app/logo1.png" # Tu logo (max 200x200px)
  
  # Configuración por plataforma
  android: true
  ios: true
  web: false
  
  # Android 12+
  android_12:
    color: "#000000"
    image: "assets/logo_app/logo1.png"
    icon_background_color: "#000000"
  
  # Mantén el splash hasta que lo remuevas manualmente
  android_gravity: center
  ios_content_mode: center
  fullscreen: true

dev_dependencies:
  build_runner: ^2.10.4
  drift_dev: ^2.29.0
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec
# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/logo_app/logo1.png
    - assets/settings/image_01.png
    - .env
    - assets/speedometer.webp
    - assets/loaders/bottle-loader.gif
    - assets/sounds/bambu_1.mp3
    - assets/sounds/favorites_03.mp3
    - assets/sounds/mixkit-select-click-1109.mp3
    - assets/loaders/loader_02.gif
    - assets/loaders/loader_03.gif
    - assets/loaders/loader_04.gif
    - assets/loaders/loader_wrong.png
    - assets/logo_app/logo_app_without_fount.png
    - assets/logo_app/logo_app_without_fount_black.png
    - assets/logo_app/logo_app_without_fount_with_words_black.png
    - assets/logo_app/logo_app_without_fount_with_words_white.png
    - assets/loaders/loader_movie_do_black.png
    - assets/loaders/loader_movie_do_white.png
    - assets/loaders/movie_do-loader.gif
    - assets/tutorial/
    - assets/images/image_without_favorites_white.png
    - assets/images/image_without_favorites_black.png

    # An image asset can refer to one or more resolution-specific "variants", see
    # https://flutter.dev/to/resolution-aware-images
    # For details regarding adding assets from package dependencies, see
    # https://flutter.dev/to/asset-from-package
    # To add custom fonts to your application, add a fonts section here,
    # in this "flutter" section. Each entry in this list should have a
    # "family" key with the font family name, and a "fonts" key with a
    # list giving the asset and other descriptors for the font. For
    # example:
    # fonts:
    #   - family: Schyler
    #     fonts:
    #       - asset: fonts/Schyler-Regular.ttf
    #       - asset: fonts/Schyler-Italic.ttf
    #         style: italic
    #   - family: Trajan Pro
    #     fonts:
    #       - asset: fonts/TrajanPro.ttf
    #       - asset: fonts/TrajanPro_Bold.ttf
    #         weight: 700
    #
    # For details regarding fonts from package dependencies,
    # see https://flutter.dev/to/font-from-package
